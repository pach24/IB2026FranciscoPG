<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Texto Normal / Regular (Peso 400) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="400"
        app:font="@font/misans_regular" />

    <!-- Texto Medio (Peso 500) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="500"
        app:font="@font/misans_medium" />

    <!-- Texto en Negrita (Peso 700) -->
    <font
        app:fontStyle="normal"
        app:fontWeight="700"
        app:font="@font/misans_bold" />

</font-family>
